// If you serve frontend from the same Express backend (recommended): leave as "".
// If you open HTML via Live Server / different port: set to "http://localhost:3000".
export const API_BASE = "";

export const STORAGE_KEYS = {
  token: "token",
  cart: "cart"
};
